# Archive

`atomic-v1/` preserves the previous library at commit
`ef792fe6d787cec7e6feb05e974070396d8dbbb7`, before the upstream-skill reset.
Moved files retain their original contents. The former root `AGENTS.md` is
named `AGENTS.original.md` so it is historical text, not active agent guidance.

This is not an installed skill collection or a runnable checkout. Its hooks,
workflows, validators, and composition rules are inactive. References retain
their old layout; the human-owned `doctrine/`, root `intent.md`, and repository
license remain at the repository root instead of being moved here.

Active skills live in `.agents/skills/`. Do not install or invoke archived skills
unless the operator explicitly requests that historical material.
